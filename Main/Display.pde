/*
  This class will take care of all the of drawing all graphs/maps/etc.
  It will be called in the main and the filtered information will be passed in, 
  which will then be used to create whatever displays are needed.
*/

/* // EXAMPLE OF HOW TO PRINT EVERY ELEMENT FROM A COlUMN SUCH AS DATE
  for(int i = 0; i < numOfRows; i++)
  {
    System.out.println(linesSplit[i][0]);
  } */
  //int highestNumber;
  
  //void drawBarChar(String[] airports, int[] values)
  //{
  //  // MAX VALUE OF Y-AXIS
  //  highestNumber = max(values);
  //  highestNumber = ((highestNumber / 10) + 1) * 10;
    
  //  // ARRAY OF OTHER VALUES
  //  int[] yAxis = new int[(highestNumber / 5) + 1];
    
  //  for(int label = 0; label < yAxis.length; label++)
  //  {
  //      yAxis[label] = label * 5; 
  //  }
    
    
  //}
